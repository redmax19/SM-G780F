name: Build Kernel

on:
  workflow_dispatch:
    inputs:
      model:
        description: 'Modelo do dispositivo (g780f, g985f, g980f, g988b, g986b, g981b)'
        required: true
        default: 'g780f'

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout do Código Fonte
        uses: actions/checkout@v7

      - name: Instalar Dependências do Sistema
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential bc libncurses5-dev libssl-dev \
            flex bison libelf-dev git ccache zip unzip bzip2

      - name: Configurar Python 2 para scripts RKP
        run: |
          sudo apt-get update
          sudo apt-get install -y python2
          sudo update-alternatives --install /usr/bin/python python /usr/bin/python2 1
          python --version

      - name: Baixar e Configurar Toolchains nas pastas esperadas pelo build.sh
        run: |
          cd ..
          mkdir -p tc
          cd tc

          echo ">> Baixando GCC 4.9 JOPP para ../tc/gcc49..."
          git clone https://github.com/afaneh-toolchain/prebuilts_gcc_linux-x86_aarch64_aarch64-linux-android-4.9-jopp \
            --depth 1 gcc49

          echo ">> Baixando Clang 8 JOPP para ../tc/clang10..."
          git clone https://github.com/afaneh-toolchain/prebuilts_clang_host_linux-x86_clang-r349610-jopp \
            --depth 1 clang10

      - name: Compilar Kernel
        run: |
          chmod +x build.sh
          ./build.sh -m ${{ github.event.inputs.model }}

      - name: Preparar e Empacotar com AnyKernel3
        run: |
          MODEL="${{ github.event.inputs.model }}"
          OUT_DIR="out_${MODEL}"
          IMG="${OUT_DIR}/arch/arm64/boot/Image"

          if [ ! -f "$IMG" ]; then
            echo "Erro: Nenhuma imagem do kernel foi encontrada em $IMG"
            exit 1
          fi

          echo ">> Imagem encontrada com sucesso: $IMG"

          echo ">> Clonando AnyKernel3..."
          git clone https://github.com/osm0sis/AnyKernel3.git --depth 1 AnyKernel3

          echo ">> Copiando Image para AnyKernel3..."
          cp "$IMG" AnyKernel3/

          cd AnyKernel3

          # Ajustar configurações do anykernel.sh para a linha S20 (Exynos 990 / universal9830)
          sed -i 's/kernel.string=.*/kernel.string=KernelSU-Next + SuSFS kernel for Galaxy S20/g' anykernel.sh
          sed -i 's/do.devicecheck=1/do.devicecheck=0/g' anykernel.sh
          sed -i 's/block=.*/auto/g' anykernel.sh
          sed -i 's/is_slot_device=1/is_slot_device=0/g' anykernel.sh

          # Criar o arquivo ZIP
          ZIP_NAME="KernelSU-Next-SuSFS-${MODEL}-$(date +%Y%m%d_%H%M%S).zip"
          zip -r9 "../${ZIP_NAME}" * -x .git README.md *placeholder

          echo "ZIP_PATH=${ZIP_NAME}" >> $GITHUB_ENV
          echo ">> Arquivo ZIP criado com sucesso: ${ZIP_NAME}"

      - name: Enviar Arquivo Zip Flashável (Artifact)
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ZIP_PATH }}
          path: ${{ env.ZIP_PATH }}
